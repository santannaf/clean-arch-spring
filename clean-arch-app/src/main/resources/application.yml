spring:
  output:
    ansi:
      enabled: ALWAYS
  h2:
    console:
      enabled: true
  datasource:
    url: jdbc:h2:mem:meta
    driverClassName: org.h2.Driver
    username: sa
    password:
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect